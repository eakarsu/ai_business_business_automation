// v0 scaffold for: Spend-category auto-classification
// Generated by batch_00 audit. Wire LLM, vendor APIs, or Prisma as needed.
import { Router, Request, Response } from 'express';
import { authenticateToken, AuthRequest } from '../middleware/auth';

const router = Router();
router.use(authenticateToken);

router.get('/health', (_req: Request, res: Response) => {
  res.json({ feature: 'spend-classifier', status: 'ok', version: 'v0' });
});

router.post('/run', async (req: AuthRequest, res: Response) => {
  try {
    const input = req.body || {};
    // TODO: wire LLM client (OpenRouter / OpenAI) or vendor integration
    return res.json({
      success: true,
      result: {
        feature: 'spend-classifier',
        receivedKeys: Object.keys(input),
        stage: 'scaffold',
        note: 'Implementation pending. See feature suggestion: Spend-category auto-classification',
      },
    });
  } catch (err: any) {
    console.error('[spend-classifier] error', err);
    return res.status(500).json({ success: false, error: err?.message });
  }
});

router.get('/history', async (_req: AuthRequest, res: Response) => {
  // TODO: persist + query from Prisma. Returning empty list for v0.
  res.json({ items: [], note: 'Persistence not wired in v0' });
});

export default router;
